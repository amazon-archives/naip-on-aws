{
  "apiRequestTemplate": {
    "application/json": {
      "state": "$input.params('state')",
      "year": "$input.params('year')",
      "quad": "$input.params('quad')",
      "scene": "$input.params('scene')"
    }
  }
}